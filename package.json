{
  "name": "portfolio-monorepo",
  "version": "1.0.0",
  "description": "Portfolio monorepo - Django backend + Next.js frontend",
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && source venv/bin/activate && python manage.py runserver 8000",
    "dev:frontend": "cd frontend && npm run dev -- --webpack",
    "build": "cd frontend && npm run build -- --webpack",
    "start": "cd frontend && npm run start",
    "install:all": "cd frontend && npm install && cd ../backend && source venv/bin/activate && pip install -r requirements.txt"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "workspaces": [
    "frontend"
  ]
}
